extends layout

block content
  nav
    a(href="/") Home

  h1.upgrade= title

  p.upgrade Upgrade your membership by entering the secret password for your desired tier

  form.upgrade(action="", method="POST")
    div.form-group
      label(for="password") Password
      input#password(type="password", name="password", required)
    button(type="submit") Submit